version: 0.2
phases:
 pre_build:
  commands:
  - echo "Entered pre-build phase"
 build:
  commands:
  - echo "Entered build phase"
  - mvn clean prepare-package war:explode
 post_build:
  commands:
  - echo "Entered the post-build phase"
  - mv target/FrontControllerDemo.war ./
artifacts:
 files:
  - FrontControllerDemo/**/*
  