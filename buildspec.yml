version: 0.2
phases:
 pre_build:
  commands:
  - echo "Entered pre-build phase"
 build:
  commands:
  - echo "Entered build phase"
  - mvn clean package
 post_build:
  commands:
  - echo "Entered the post-build phase"
  - cp target/FrontControllerDemo.war $CATALINE_HOME/webapps
artifacts:
 files:
  - target/**/*
  